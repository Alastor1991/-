# 🔥 HELL'S HUB (Адский Хаб) — Локальная Версия

Добро пожаловать в репозиторий **Hell's Hub**. Это полностью рабочий React-проект с симуляцией бекенда (Local-First), который вы можете запустить на своем компьютере.

## 📋 Требования перед запуском

Для работы проекта вам **необходим** установленный **Node.js**. Это среда, которая запускает JavaScript вне браузера.

---

## 🖥️ Инструкция для Windows

### 1. Установка Node.js
1. Перейдите на официальный сайт: [nodejs.org](https://nodejs.org/).
2. Скачайте версию **LTS** (Recommended for most users).
3. Установите её, нажимая "Next". **Важно:** Убедитесь, что все галочки стоят, особенно `Add to PATH` и `npm package manager`.
4. После установки **обязательно перезагрузите компьютер**.

### 2. Запуск проекта
1. Распакуйте архив с проектом.
2. Зайдите в папку проекта.
3. Нажмите **Shift + Правая кнопка мыши** на пустом месте в папке -> выберите **"Открыть окно PowerShell здесь"** (или "Открыть в Терминале").
4. Введите команду для установки библиотек:
   ```powershell
   npm install
   ```
5. После завершения (появится папка `node_modules`) запустите проект:
   ```powershell
   npm run dev
   ```
6. Скопируйте ссылку из терминала (обычно `http://localhost:5173`) и вставьте в браузер.

---

## 🐧 Инструкция для Arch Linux / Manjaro

*I use Arch btw.*

### 1. Подготовка
Откройте терминал и выполните:
```bash
sudo pacman -Syu nodejs npm
```

### 2. Запуск
1. Перейдите в папку проекта:
   ```bash
   cd /путь/к/папке/проекта
   ```
2. Если ранее возникали ошибки с версиями, удалите старые лок-файлы (опционально):
   ```bash
   rm -rf node_modules package-lock.json
   ```
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Запустите сервер:
   ```bash
   npm run dev
   ```

---

## 🛠 Решение проблем (Troubleshooting)

**Ошибка в PowerShell: "npm не является командлетом..."**
*   Вы не перезагрузили ПК после установки Node.js.

**Черный/Белый экран при открытии в браузере**
*   Попробуйте обновить страницу через `Ctrl + F5`.
